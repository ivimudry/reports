{% assign p = trigger.phone | remove: '+' | remove: ' ' | remove: '-' | remove: '(' | remove: ')' | remove: '_' | remove: '.' | remove: '/' %}{% assign plen = p | size %}{% assign fc = p | slice: 0 %}{% if fc == '0' and plen == 10 %}{% assign p = p | slice: 1, 9 | prepend: '0027' %}{% else %}{% assign p = p | prepend: '00' %}{% endif %}{
    "messages": {
        "msg": [
            {
                "allowedChannels": ["SMS"],
                "from": "PantherBet",
                "to": [
                    {
                        "number": "{{p}}"
                    }
                ],
                "minimumNumberOfMessageParts": 1,
                "maximumNumberOfMessageParts": 3,
                "body": {
                    "type": "auto",
                    "content": "{{trigger.message}}"
                },
                "reference": "cio-{{p}}-{{'now' | date: '%Y%m%d%H%M%S'}}"
            }
        ]
    }
}